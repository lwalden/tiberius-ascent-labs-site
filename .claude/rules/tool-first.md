Use tools instead of asking the user. Install CLI tools if needed. Query cloud services via CLI. Use Playwright for browser interaction.

Auth exception: if a CLI requires interactive login, ask the user to authenticate, then proceed.
